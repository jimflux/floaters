export const metadata = {
  title: "Floaters",
  description: "Cash flow forecasting API",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
