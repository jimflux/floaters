# Web app build-time config (Vite). Copy to .env for local dev.
# These are embedded in the client bundle — not secrets.

# Base URL of the Floaters API. Point at Railway once migrated.
VITE_API_URL=http://localhost:3000

# API key (must match the API's CONNECT_SECRET).
VITE_API_KEY=
